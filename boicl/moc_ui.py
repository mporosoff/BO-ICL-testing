"""Compact campaign view hosted by the existing standard-library local app."""
MOC_HTML = r"""<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>MoC continuation · BO-ICL</title>
<style>
:root{font-family:Segoe UI,system-ui,sans-serif;color:#142d36;background:#f3f6f6}*{box-sizing:border-box}body{max-width:1180px;margin:auto;padding:28px}h1{font-size:28px;margin:4px 0}h2{font-size:19px;margin-top:0}header{display:flex;justify-content:space-between;align-items:center;margin-bottom:24px}.sub{color:#536a73;font-size:14px}section{background:white;border:1px solid #dbe4e4;border-radius:12px;padding:22px;margin-bottom:18px}.row{display:flex;align-items:end;gap:14px;flex-wrap:wrap}.row>label{flex:1;min-width:160px}label{display:block;font-size:13px;font-weight:600;margin:8px 0}input,select,textarea,button{font:inherit}input,select,textarea{display:block;width:100%;padding:10px;border:1px solid #b8c9ce;border-radius:6px;margin-top:5px;background:#fff;color:#142d36}button,.button{padding:10px 16px;background:#087e80;border:0;border-radius:6px;color:white;cursor:pointer;display:inline-block;text-decoration:none}button.secondary{background:#e4eeee;color:#163d46}button:disabled{opacity:.5;cursor:default}.badge{padding:5px 10px;border-radius:15px;background:#e3f2ed;font-size:12px;display:inline-block}.warning{background:#fff1cf;padding:12px;border-radius:6px}.stats{display:flex;gap:36px;flex-wrap:wrap;margin:16px 0}.stats b{display:block;font-size:26px}.stats span{font-size:13px;color:#50676f}details{margin-top:18px}summary{cursor:pointer;font-weight:600}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-family:inherit;font-size:14px}table{border-collapse:collapse;width:100%;font-size:13px}td,th{text-align:left;border-bottom:1px solid #dde5e6;padding:9px}#notice{position:sticky;top:8px;z-index:1;background:#edf7f7;border:1px solid #b9d8d8;padding:12px;border-radius:6px;margin-bottom:16px}#notice:empty{display:none}.hidden{display:none!important}.layout{display:grid;grid-template-columns:1.3fr 1fr;gap:18px}textarea{min-height:110px}.muted{color:#61757b}.recipe{font-size:14px;line-height:1.6}.values{display:flex;gap:22px;flex-wrap:wrap;margin:12px 0}a{color:#087e80}@media(max-width:960px){.layout{grid-template-columns:1fr}body{padding:15px}}
</style><header><div><span class="sub">BO-ICL · LOCAL CAMPAIGNS</span><h1>MoC continuation</h1><div class="sub">Suggest an experiment. Record its measured result. Continue.</div></div><a id="mainView" href="/">Main toolkit interface →</a></header>
<div id="notice" role="status"></div><div id="demo" class="warning hidden">SYNTHETIC DEMONSTRATION — provider responses and any new measurements are demonstration fixtures. No model API calls.</div>
<section><h2>1 · Campaign and data</h2><div class="row"><label>Saved campaign<select id="campaign"><option value="">Create or resume a campaign</option></select></label><button class="secondary" id="resume">Resume</button><button class="secondary" id="openCampaignTab">Open selected in new tab</button><a id="export" class="button" href="#">Export bundle</a><label>Import saved bundle<input id="bundleFile" type="file" accept=".json"></label></div>
<details id="checkpointPanel" class="hidden"><summary>Saved checkpoints</summary><p class="sub">Changes save automatically. Resume a checkpoint as an independent campaign copy with its measurements, settings and pending reservations.</p><div class="row"><label>Checkpoint label (optional)<input id="checkpointName"></label><button class="secondary" id="saveCheckpoint">Save checkpoint now</button></div><label>Saved point<select id="checkpointSelect"></select></label><div class="row"><button class="secondary" id="refreshCheckpoints">Refresh list</button><button id="restoreCheckpoint">Resume selected as independent copy</button></div></details>
<details id="createPanel" open><summary>Load MoC continuation package</summary><p class="sub">The included source data contains 7,776 recipes and the confirmed M7, M12, M13 observations. Optionally select the supplied workbook; its named sheets are validated together. Preset creation makes a new campaign. Use Resume for a saved campaign. Import makes no model calls. The selected preset supplies its budget. A deliberate blank override means unlimited; zero prevents new suggestions.</p><div class="row"><label>Method preset<select id="preset"><option value="moc_five_gp">MoC five-point comparison — six-variable GP</option><option value="moc_five_llm">MoC five-point comparison — BO-ICL LLM</option></select></label><label>New-measurement budget<input id="budget" type="number" min="0" placeholder="No limit"></label><label>Optional source workbook<input type="file" id="workbook" accept=".xlsx"></label></div><p id="creationPresetLabel" class="sub"></p><details><summary>Selected preset and complete effective settings</summary><pre id="creationConfig"></pre></details><div class="row"><button id="create" disabled>Create new campaign</button><button class="secondary" id="pair" disabled>Create five-point matched pair</button></div><p class="sub">The matched pair uses the two five-point presets: three shared seeds, five new syntheses per arm, GP EI from three measured designs. Any deliberate budget override applies to both arms.</p><details><summary>Matched pair effective settings</summary><pre id="pairConfig"></pre></details></details></section>
<div id="active" class="hidden"><section><div class="row"><h2>2 · Method</h2><span id="engine" class="badge"></span><span id="campaignIdentity" class="sub"></span></div><div id="stats" class="stats"></div><p id="stage"></p><div class="row"><label>Primary method<select id="method"><option value="llm">BO-ICL (LLM)</option><option value="gp">Gaussian process</option></select></label><label>Refresh suggestion after saving a measurement<input id="auto" type="checkbox" style="width:auto" title="After deliberate start, saving a measurement may trigger paid LLM or embedding requests."></label></div>
<details id="settingsPanel"><summary>Advanced settings and effective configuration</summary><div id="gpSettings"><label>GP input representation<select id="representation"><option value="gpr_features">Synthesis parameters</option><option value="gpr_embeddings">Text embeddings</option></select></label><p id="gpExplain" class="sub"></p><label id="thresholdField">Start EI after distinct measured designs<input id="threshold" type="number" min="2" step="1"></label></div><div id="llmSettings"><div class="row"><label>Prediction model<input id="forwardModel"></label><label>Inverse model<input id="inverseModel"></label><label>Predictions per candidate<input id="samples" type="number" min="2"></label></div><div class="row"><label>Observed examples<select id="examples"><option value="nearest">Nearest observed (up to five)</option><option value="all">All observed examples</option></select></label><label>Uncertainty multiplier<input id="scalar" type="number" min="0" step=".1"></label></div><p class="sub">One inverse query per step, one completion per query. Nearest 100 eligible recipes → MMR 16 → five forward predictions. EI is in percentage points. An automatic target uses best + max(0, multiplier − 1) × |best|, bounded to 0–100.</p><details><summary>Inverse target and standalone proposals</summary><label>Manual inverse target (blank = automatic)<input id="manualTarget" type="number" step="any" placeholder="automatic"></label><label>Standalone proposal count<input id="inverseCount" type="number" min="1" max="20" value="1"></label><p class="sub">Zero is an explicit target in wt%. A manual target is not multiplied again. Ordinary BO uses one inverse completion; this separate count applies only to standalone proposals.</p></details><label>Custom prediction system message (blank uses managed prompt)<textarea id="forwardPrompt"></textarea></label><label>Custom inverse system message (blank uses managed prompt)<textarea id="inversePrompt"></textarea></label></div><p id="settingsDraftStatus" class="sub"></p><button class="secondary" id="discardSettings">Discard settings draft</button><button class="secondary" id="previewSettings">Preview settings changes</button><pre id="configPreview"></pre><button id="applySettings" class="hidden">Apply reviewed settings</button><details><summary>Saved preset and resolved settings</summary><p id="savedPresetLabel"></p><pre id="resolved"></pre></details><details><summary>Explicitly apply or reset a preset</summary><p class="sub">Preview a factory preset, then explicitly apply it. This resets method settings and MoC prompts while preserving measurement history, reservations and the recorded measurement-definition decision.</p><label>Preset to apply<select id="resetPreset"></select></label><button class="secondary" id="previewPresetReset">Preview preset reset</button></details></details></section>
<section><h2>Live progress</h2><p class="sub">The main toolkit graph shows initialization in supplied order at i1, i2, i3, …, shaded and separated by a divider from BO steps 1, 2, … . Initialization is not BO-selected and does not consume the new-measurement budget. Pending predictions remain separate from measured outcomes.</p><iframe id="sharedGraph" title="Shared campaign live graph" style="width:100%;height:440px;border:0;border-radius:8px" loading="lazy"></iframe></section><div class="layout"><section><h2>3 · Suggest experiment</h2><div class="row"><button id="suggest">Start suggestion</button><button class="secondary" id="cancelJob">Cancel current work</button></div><p id="progress" role="status" class="sub"></p><div id="proposal"></div><details id="standalonePanel"><summary>Standalone inverse proposals</summary><p class="sub">Apply or discard settings drafts first. Generating proposals uses saved settings and can use paid model calls. Free-form text is saved separately and never becomes a candidate, reservation or measurement.</p><button class="secondary" id="generateInverse">Generate standalone proposals</button><div id="inverseRecords"></div></details><details id="requestRequestPreview"><summary>Preview full LLM request (no model calls)</summary><p class="hint">Uses saved settings. Choose a candidate for a forward preview. Next BO inverse request uses one completion; Standalone inverse proposal uses its separate saved count and sequence. A recorded step uses its stored request. Missing cached selector inputs or an unknown future shortlist are reported as unresolved; preview does not generate embeddings.</p><label>Request role<select id="requestPreviewRole"><option value="forward">Forward prediction</option><option value="bo_inverse">Next BO inverse request</option><option value="standalone_inverse">Standalone inverse proposal</option></select></label><label>Request source<select id="requestPreviewSource"><option value="current">Current saved settings</option><option value="recorded">Recorded suggestion selected below</option></select></label><label>Find candidate by ID or procedure<input id="requestPreviewQuery"></label><button class="secondary" id="requestPreviewSearch">Find candidates</button><label>Selected candidate<select id="requestPreviewCandidate"></select></label><button class="secondary" id="requestPreview">Preview request</button><pre id="requestPreviewResult"></pre></details><details id="cachePanel"><summary>Embedding cache transfer</summary><p class="sub">Import a safe cache package from a local folder, or prepare only missing vectors. Apply or discard settings drafts before cache actions. Preparing embeddings uses saved settings and is an intentional live API action. Structured GP needs none.</p><label>Cache package folder<input id="cachePath" placeholder="Absolute local folder containing manifest.json"></label><div class="row"><button class="secondary" id="cacheImport">Validate and import cache</button><button class="secondary" id="cachePrepare">Prepare missing embeddings</button></div><pre id="cacheReport"></pre></details></section>
<section><h2>4 · Record measurement</h2><p class="sub">Reserve a recommendation, perform and measure the synthesis, then enter the actual result. A prediction never becomes a measurement automatically.</p><label>Pending experiment<select id="pending"></select></label><label>Measured MoC (wt%)<input id="value" type="number" min="0" max="100" step="any"></label><details id="measurementQuality"><summary>Measurement quality</summary><div class="row"><label>XRD esd (percentage points)<input id="sigma" type="number" min="0" step="any"></label><label>GOF<input id="gof" type="number" min="0" step="any"></label><label>Closure gap (percentage points)<input id="gap" type="number" step="any"></label></div><p class="hint">Confirmed values do not establish a quantification method. Keep unknown historical methods unspecified. Record the actual method and normalization; mass fractions and pattern-area fractions are not interchangeable.</p><label>Quantification method<select id="quality_quantification_method"><option value="historical_unspecified">Historical / unspecified (unknown)</option><option value="gsas_ii_mass_fraction">GSAS-II mass fraction</option><option value="xrd_area_fraction">Integrated phase-pattern area fraction</option><option value="other">Other documented method</option></select></label><label>Normalization basis<input id="quality_normalization"></label><label>Source file<input id="quality_source_file"></label><label>Source record identifier<input id="quality_source_identifier"></label><label>Refinement identifier<input id="quality_refinement_id"></label><label>Uncertainty method / provenance<input id="quality_uncertainty_method"></label><label>Definition note<input id="quality_definition_note"></label><details><summary>Measurement definition and historical training</summary><p class="hint">Document a validated definition before mixing explicit methods with historical observations. This decision preserves original numbers and provenance. Incompatible explicit definitions are excluded from training; unknown historical records need exclusion or a scientific justification to retain them.</p><pre id="qualityStatus"></pre><label>Validated definition<select id="definitionMethod"><option value="">Choose validated method</option><option value="gsas_ii_mass_fraction">GSAS-II mass fraction</option><option value="xrd_area_fraction">Integrated phase-pattern area fraction</option><option value="other">Other documented method</option></select></label><label>Normalization basis<input id="definitionNormalization"></label><label>Definition note (required for other methods)<input id="definitionNote"></label><label>Unknown historical records<select id="definitionHistoricalPolicy"><option value="unresolved">Unresolved — choose a policy</option><option value="exclude">Exclude from model training</option><option value="retain_with_justification">Retain with scientific justification</option></select></label><label>Scientific decision / reason<input id="definitionReason"></label><button class="secondary" id="applyDefinition">Record definition decision</button></details><label>Gap source / override reason<input id="gapOrigin" placeholder="Required; distinguish an override from a computed gap"></label><div class="row"><label>Mo (wt%, optional)<input id="moPhase" type="number" min="0" max="100" step="any"></label><label>Mo2C (wt%, optional)<input id="mo2cPhase" type="number" min="0" max="100" step="any"></label><label>MoO2 (wt%, optional)<input id="moo2Phase" type="number" min="0" max="100" step="any"></label></div><label>Reported phase accounting is complete<input id="phasesComplete" type="checkbox" style="width:auto"></label></details><label>Measurement note<input id="note"></label><div class="row"><button id="measure">Save confirmed measurement</button><button id="release" class="secondary">Release reservation</button></div></section></div>
<section><h2>5 · History and provenance</h2><div id="history"></div><details><summary>Source decisions and excluded historical measurements</summary><p>M12 = <strong>83.8 wt%</strong>, confirmed on 20 September 2026. The older 81.7 value is superseded provenance. Source gap overrides remain zero; M12's phase accounting residual is −0.3 points. Missing metadata is never a measured zero.</p><p id="policy"></p><pre id="provenance"></pre></details><details><summary>Exact requests, prediction samples, targets and replay</summary><label>Recorded suggestion<select id="replayStep"></select></label><button class="secondary" id="replay">Replay recorded step offline</button><pre id="replayResult"></pre></details><details><summary>Revise a measurement refinement</summary><p class="sub">This retains the physical measurement ID and preserves the old refinement as superseded. Use the measurement fields above for its revised values.</p><label>Observation<select id="reviseObservation"></select></label><label>Revision reason<input id="revisionReason"></label><button class="secondary" id="loadRefinement">Load observation into measurement fields</button><button class="secondary" id="revise">Save refinement revision</button></details></section></div>
<script>
const $=id=>document.getElementById(id);let current=new URLSearchParams(location.search).get('campaign')||localStorage.getItem('boicl_shared_campaign_id')||'',state=null,poll=null,settings=null,settingsCampaign=null,settingsSnapshot=null,settingsAction='config',settingsPreset=null,settingsBase=null,settingsBaseCampaign=null,settingsReviewSequence=0,loadSequence=0,campaignEpoch=0,presetSequence=0,creationPreview=null,pairPreview=null,budgetOverride=false,creationBusy=false,settingsSavePending=false;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function fmt(v){return typeof v==='number'&&Number.isFinite(v)?v.toFixed(2):'—';}
function targetHtml(t){if(!t)return '';return `<p><b>Inverse target:</b> best ${fmt(t.best)}; multiplier ${fmt(t.multiplier_draw)}; raw ${fmt(t.raw_target)} → resolved ${fmt(t.resolved_target)} wt%.</p><details><summary>Target calculation and bounds</summary><pre>${esc(JSON.stringify(t,null,2))}</pre></details>`;}
function predictionHtml(r){const p=r.prediction;if(!p)return '<p>No model prediction is attached to this selection.</p>';let text=`<p><b>Predicted MoC:</b> ${fmt(p.mean)} wt%</p>`;if(p.lower95!=null)text+=`<p><b>95% latent-function interval:</b> ${fmt(p.lower95)}–${fmt(p.upper95)} wt%</p>`;if(p.accepted_samples!=null)text+=`<p><b>Accepted predictions:</b> ${p.accepted_samples} / ${p.requested_samples??state.config.llm.n_samples}. ${p.partial_samples?'Partial sample set. ':''}${p.std===0?'Agreement among responses; this does not establish accuracy.':'Completion spread: '+fmt(p.std)+' percentage points.'}</p>`;text+=`<p><b>Acquisition:</b> ${fmt(r.score)} ${esc(r.acquisition_units||'')}</p>`;text+=`<details><summary>Prediction diagnostics</summary><pre>${esc(JSON.stringify(p,null,2))}</pre></details>`;return text;}
async function api(action,body={},method='POST'){const r=await fetch('/api/moc/'+action,{method,headers:{'Content-Type':'application/json'},body:method==='GET'?undefined:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw Error(data.error||'Request failed');return data;}
function notice(t){$('notice').textContent=t;}async function guarded(fn){try{await fn();}catch(e){notice(e.message);if(/esd|sigma|gof|gap|quality|quantification|definition|historical/i.test(e.message))$('measurementQuality').open=true;}}
async function catalog(){const requested=current,epoch=campaignEpoch;const r=await api('list',{},'GET');if(requested!==current||epoch!==campaignEpoch)return;r.campaigns=r.campaigns.filter(c=>c.data_schema!=='generic');$('demo').classList.toggle('hidden',!r.demo);$('campaign').innerHTML='<option value="">Select a saved campaign</option>'+r.campaigns.map(c=>`<option value="${esc(c.campaign_id)}">${esc(c.name)} · ${esc(c.campaign_id.slice(0,8))}${c.selection_policy==='random_control'?' · RANDOM CONTROL':''}${c.synthetic_demo?' · DEMO':''}</option>`).join('');if(current&&r.campaigns.some(c=>c.campaign_id===current))$('campaign').value=current;else if(current)switchCampaign('');}
function toggle(){const llm=$('method').value==='llm';$('llmSettings').classList.toggle('hidden',!llm);$('gpSettings').classList.toggle('hidden',llm);$('thresholdField').classList.toggle('hidden',$('representation').value!=='gpr_features');$('cachePanel').classList.toggle('hidden',!llm&&$('representation').value==='gpr_features');$('standalonePanel').classList.toggle('hidden',!llm);$('requestRequestPreview').classList.toggle('hidden',!llm);$('gpExplain').textContent=$('representation').value==='gpr_features'?'Six fixed synthesis features. Matérn 5/2 ARD, quality-dependent observation noise, bounded posterior. No credentials or embeddings.':`Procedure-only ${state?.config.embedding_gp.embedding_model||'text-embedding-ada-002'} embeddings, fixed full-corpus Isomap and learned homoskedastic noise. Its ordinary Gaussian posterior and raw-unit EI are unbounded; 0–100 plot limits affect display only.`;}
function switchCampaign(id){current=id;state=null;$('active').classList.add('hidden');$('checkpointPanel').classList.add('hidden');campaignEpoch++;loadSequence++;clearTimeout(poll);settingsBase=null;settingsBaseCampaign=null;invalidateSettingsPreview();}
async function load(){
 if(!current)return;
 const requested=current,epoch=campaignEpoch,sequence=++loadSequence;
 localStorage.setItem('boicl_shared_campaign_id',requested);history.replaceState(null,'','/moc?campaign='+encodeURIComponent(requested));$('mainView').href='/?campaign='+encodeURIComponent(requested);
 const loaded=await api('state?id='+encodeURIComponent(requested),{},'GET');
 if(requested!==current||epoch!==campaignEpoch||sequence!==loadSequence)return;
 state=loaded;
 if($('checkpointPanel').dataset.campaign!==current){
  invalidateSettingsPreview();
  for(const field of ['value','sigma','gof','gap','gapOrigin','moPhase','mo2cPhase','moo2Phase','note','revisionReason','definitionReason','definitionNote','requestPreviewQuery'])$(field).value='';
  $('phasesComplete').checked=false;fillQualityValues();$('requestPreviewResult').textContent='';$('requestPreviewCandidate').innerHTML='';$('pending').innerHTML='';$('checkpointPanel').open=false;$('checkpointPanel').dataset.campaign=current;$('checkpointSelect').innerHTML='';
 }
 $('checkpointPanel').classList.remove('hidden');render();
}
function render(){const s=state,c=s.config;$('active').classList.remove('hidden');$('createPanel').open=false;$('engine').textContent=c.selection_policy==='random_control'?'Independent random control':s.engine_label;$('campaignIdentity').textContent=c.name+' · '+s.campaign_id;$('export').href='/api/moc/export?id='+current;$('stats').innerHTML=[['Measured',s.counts.measured],['Pending',s.counts.pending],['Available',s.counts.available],['Best measured',s.best==null?'—':s.best+' wt%']].map(([k,v])=>`<div><b>${esc(v)}</b><span>${k}</span></div>`).join('');$('stage').textContent=c.selection_policy==='random_control'?'Uniform random selection; only this independent arm’s confirmed measurements enter its history.':c.engine==='gpr_features'?(s.counts.unique_measured<c.structured_gp.ei_after_unique_measured_designs?`Space-filling: ${s.counts.unique_measured} of ${c.structured_gp.ei_after_unique_measured_designs} measured designs. Selection seeks coverage.`:`Expected improvement across all eligible designs; xi ${c.structured_gp.ei_xi_standardized_logit} standardized logit units.`):'Full eligible pool; '+(c.engine==='llm'?c.llm.acquisition.replaceAll('_',' ')+' from the empirical completion distribution.':'latent-function EI, in raw objective units.');syncSettingsForm(c,current);$('inverseRecords').textContent=JSON.stringify(s.inverse_proposals||[],null,2);$('qualityStatus').textContent=JSON.stringify(s.quality_status||{},null,2);syncDefinitionForm(c.measurement_definition||{},current);$('savedPresetLabel').textContent=(c.preset_provenance?.display_name||c.preset)+' · version '+(c.preset_version||'legacy-unversioned')+' · saved configuration (including overrides)';$('resolved').textContent=JSON.stringify(c,null,2);const graphKey=current+':'+s.history_revision+':'+s.counts.pending+':'+s.suggestions.length;if($('sharedGraph').dataset.key!==graphKey){$('sharedGraph').src='/campaign-graph?campaign='+encodeURIComponent(current);$('sharedGraph').dataset.key=graphKey;}toggle();$('progress').textContent=s.progress?.status?`${s.progress.status}: ${s.progress.detail||''}`:'';updateDraftStatus();
const latest=[...s.suggestions].reverse().find(x=>x.status==='suggested'||x.status==='pending');$('proposal').innerHTML=latest?`<hr><span class="badge">${esc(latest.status)}${latest.planned_quality_repeat?' · planned quality repeat':''}</span><h3>${esc(latest.candidate_id)}</h3><p><b>Selection reason:</b> ${esc(latest.selection_reason)}</p><p class="recipe">${esc(latest.candidate.procedure)}</p><div class="values">${[['temperature_C','Temperature (°C)'],['ramp_C_per_min','Ramp (°C/min)'],['gas','Gas'],['flow_sccm','Flow (sccm)'],['hold_h','Hold (h)'],['sucrose_to_AMT_mass_ratio','Sucrose:AMT']].map(([k,label])=>`<span>${label}: <b>${esc(latest.candidate[k])}</b></span>`).join('')}</div>${targetHtml(latest.inverse_target)}${predictionHtml(latest)}${latest.status==='suggested'?`<button onclick="reserve('${latest.suggestion_id}')">Reserve this experiment</button>`:''}`:'<p class="muted">Start a suggestion to choose an eligible recipe.</p>';
const previousPending=$('pending').value;$('pending').innerHTML=s.suggestions.filter(r=>r.status==='pending').map(r=>`<option value="${r.suggestion_id}">${esc(r.candidate_id)}</option>`).join('');if(previousPending)$('pending').value=previousPending;$('measure').disabled=!$('pending').value;$('release').disabled=!$('pending').value;$('history').innerHTML='<table><thead><tr><th>Run / measurement</th><th>MoC wt%</th><th>XRD esd</th><th>GOF</th><th>Gap</th><th>Status</th></tr></thead><tbody>'+s.observations.map(o=>`<tr><td>${esc(o.run_id||o.observation_id.slice(0,12))}${o.synthetic?' · synthetic':''}</td><td>${o.moc_wt_pct}</td><td>${o.moc_wt_pct_sigma??'unknown'}</td><td>${o.gof??'unknown'}</td><td>${o.closure_gap_wt_pct??'unknown'}</td><td>${esc(o.record_status)}${o.training_included===false?' · excluded from training':''}<details><summary>Quantification provenance</summary><pre>${esc(JSON.stringify(o.measurement_quality||{quantification_method:'historical_unspecified'},null,2))}</pre></details></td></tr>`).join('')+'</tbody></table>';$('provenance').textContent=JSON.stringify({resolution:s.provenance.m12_resolution,archived:s.archive.map(o=>({id:o.observation_id,value:o.moc_wt_pct,excluded:!o.training_included,phase_residual:o.phase_accounting_residual_wt_pct}))},null,2);$('policy').textContent='Repeat policy: '+c.repeat_policy+'. '+(c.initialization==='eight_observations'?'The five historical measurements are included in this explicitly selected eight-observation initialization.':'Five archived measurements remain known history and are excluded from the matched models.')+' Later histories are independent.';const previousReplay=$('replayStep').value;$('replayStep').innerHTML=[...s.suggestions,...(s.inverse_proposals||[])].map(r=>`<option value="${r.suggestion_id||r.proposal_id}">${esc(r.candidate_id||(r.proposal_id?'Standalone inverse':r.status))} · ${esc(r.status)}</option>`).join('');if(previousReplay)$('replayStep').value=previousReplay;const previousRevision=$('reviseObservation').value;$('reviseObservation').innerHTML=s.observations.filter(o=>o.record_status==='measured').map(o=>`<option value="${o.observation_id}">${esc(o.run_id||o.observation_id)}</option>`).join('');if(previousRevision)$('reviseObservation').value=previousRevision;clearTimeout(poll);if(s.progress?.status==='running'){const requested=current,epoch=campaignEpoch;poll=setTimeout(()=>{if(requested===current&&epoch===campaignEpoch)guarded(load);},1000);}}
function updateCreationButtons(){const blocked=creationBusy||!creationPreview||!pairPreview;$('create').disabled=blocked;$('pair').disabled=blocked;}
async function create(pair){
 if(creationBusy)throw Error('Campaign creation is already in progress.');
 const requested=current,epoch=campaignEpoch;creationBusy=true;updateCreationButtons();
 try{
  await previewCreation(false);
  const chosen=creationPreview,paired=pairPreview;
  if(!chosen||!paired)throw Error('Validate the selected preset before creation.');
  const body={preset:chosen.preset,workbook:null};if(pair)body.study='five_point';
  if(budgetOverride)body.budget=chosen.config.new_measurement_budget;
  const file=$('workbook').files[0];if(file){const buf=new Uint8Array(await file.arrayBuffer());let binary='';for(let i=0;i<buf.length;i+=32768)binary+=String.fromCharCode(...buf.subarray(i,i+32768));body.workbook=btoa(binary);}
  if(requested!==current||epoch!==campaignEpoch)return;
  const r=await api(pair?'pair':'create',body);
  if(requested!==current||epoch!==campaignEpoch){notice('Campaign creation completed. Resume the new campaign from Saved campaign: '+(pair?r.gp:r.campaign_id));return;}
  switchCampaign(pair?r.gp:r.campaign_id);await catalog();await load();notice(pair?'Created independent five-point matched campaigns. Choose either in Saved campaign.':'Validated 7,776 candidates and initialized live measurements. No model calls.');
 }finally{creationBusy=false;updateCreationButtons();}
}
async function loadCheckpoints(){if(!current)return;const requested=current,previous=$('checkpointSelect').value;const result=await api('checkpoints?id='+encodeURIComponent(requested),{},'GET');if(requested!==current)return;$('checkpointSelect').innerHTML=result.checkpoints.map(r=>`<option value="${esc(r.checkpoint_id)}">${esc(r.created_at)} · ${esc(r.name||r.reason||'Saved point')} · ${r.observation_count} measured / ${r.pending_count} pending</option>`).join('');if(result.checkpoints.some(r=>r.checkpoint_id===previous))$('checkpointSelect').value=previous;$('restoreCheckpoint').disabled=!result.checkpoints.length;}
$('openCampaignTab').onclick=()=>guarded(async()=>{const id=$('campaign').value;if(!id)throw Error('Select a saved campaign first.');window.open('/moc?campaign='+encodeURIComponent(id),'_blank','noopener');});$('checkpointPanel').ontoggle=()=>{if($('checkpointPanel').open)guarded(loadCheckpoints);};$('refreshCheckpoints').onclick=()=>guarded(loadCheckpoints);$('saveCheckpoint').onclick=()=>guarded(async()=>{await api('checkpoint',{id:current,name:$('checkpointName').value||null});await loadCheckpoints();notice('Checkpoint saved.');});$('restoreCheckpoint').onclick=()=>guarded(async()=>{const checkpoint_id=$('checkpointSelect').value;if(!checkpoint_id)throw Error('Select a saved checkpoint.');const result=await api('restore-checkpoint',{id:current,checkpoint_id});switchCampaign(result.campaign_id);await catalog();await load();notice('Opened an independent campaign copy from the selected checkpoint.');});
$('create').onclick=()=>guarded(()=>create(false));$('pair').onclick=()=>guarded(()=>create(true));$('resume').onclick=()=>guarded(async()=>{switchCampaign($('campaign').value);await load();notice('Campaign resumed with measurements and pending reservations preserved.');});$('suggest').onclick=()=>guarded(async()=>{requireSavedSettings();await api('suggest',{id:current});await load();});$('cancelJob').onclick=()=>guarded(async()=>{await api('cancel',{id:current});await load();});window.reserve=id=>guarded(async()=>{await api('reserve',{id:current,suggestion_id:id});await load();});$('release').onclick=()=>guarded(async()=>{await api('release',{id:current,suggestion_id:$('pending').value});await load();});
function values(){return {moc_wt_pct:$('value').value,moc_wt_pct_sigma:$('sigma').value,gof:$('gof').value,closure_gap_wt_pct:$('gap').value,closure_gap_origin:$('gapOrigin').value,source_note:$('note').value,mo_wt_pct:$('moPhase').value,mo2c_wt_pct:$('mo2cPhase').value,moo2_wt_pct:$('moo2Phase').value,phase_accounting_complete:$('phasesComplete').checked,...qualityValues()};}
$('measure').onclick=()=>guarded(async()=>{await api('measure',{id:current,suggestion_id:$('pending').value,values:values(),request_id:crypto.randomUUID()});await load();notice('Measurement saved atomically with its quality metadata.');});$('revise').onclick=()=>guarded(async()=>{await api('refine',{id:current,observation_id:$('reviseObservation').value,values:values(),reason:$('revisionReason').value});await load();notice('New refinement saved; old record retained and previous suggestions invalidated.');});function methodChanged(){invalidateSettingsPreview();toggle();$('settingsPanel').open=true;updateDraftStatus();}
const settingsFields=['method','representation','auto','forwardModel','inverseModel','samples','manualTarget','inverseCount','scalar','examples','forwardPrompt','inversePrompt','threshold'];
function settingsValues(){return Object.fromEntries(settingsFields.map(id=>[id,id==='auto'?$(id).checked:$(id).value]));}
function settingsFingerprint(){return JSON.stringify(settingsValues());}
function settingsDirty(){return settingsBaseCampaign===current&&settingsBase!==null&&settingsFingerprint()!==settingsBase;}
function invalidateSettingsPreview(){++settingsReviewSequence;settings=null;settingsCampaign=null;settingsSnapshot=null;settingsPreset=null;$('applySettings').classList.add('hidden');$('configPreview').textContent='';}
function requireSavedSettings(){if(settingsSavePending)throw Error('Wait for the settings save to complete before starting this action.');if(settingsDirty())throw Error('Apply reviewed settings or discard the settings draft before starting this action.');}
function updateDraftStatus(){
 const dirty=settingsDirty(),blocked=dirty||settingsSavePending;
 $('settingsDraftStatus').textContent=settingsSavePending?'Saving reviewed settings — actions are unavailable until the save completes.':dirty?'Unsaved settings draft — polling preserves these edits. Apply reviewed settings or discard before starting suggestions, standalone proposals or embedding actions.':'';
 $('suggest').disabled=blocked||state?.progress?.status==='running';
 for(const id of ['generateInverse','cachePrepare','cacheImport'])$(id).disabled=blocked;
 $('applySettings').disabled=settingsSavePending;
}
function syncSettingsForm(c,campaignId,force=false){
 if(!force&&settingsBaseCampaign===campaignId&&settingsDirty()){updateDraftStatus();return;}
 const values=settingsConfigValues(c);
 for(const [id,value] of Object.entries(values)){if(id==='auto')$(id).checked=value;else $(id).value=value;}
 settingsBaseCampaign=campaignId;settingsBase=settingsFingerprint();updateDraftStatus();
}
function settingsConfigValues(c){
 const values={method:c.engine==='llm'?'llm':'gp',representation:c.engine==='gpr_embeddings'?'gpr_embeddings':'gpr_features',auto:c.auto_suggest,forwardModel:c.llm.forward_model,inverseModel:c.llm.inverse_model,samples:c.llm.n_samples,manualTarget:c.llm.manual_inverse_target??'',inverseCount:c.llm.inverse_proposal_count??1,scalar:c.llm.uncertainty_scalar,examples:c.llm.selector_mode,forwardPrompt:c.llm.forward_system_message??'',inversePrompt:c.llm.inverse_system_message??'',threshold:c.structured_gp.ei_after_unique_measured_designs};
 return Object.fromEntries(Object.entries(values).map(([id,value])=>[id,id==='auto'?value:String(value)]));
}
function settingsChanges(){return {engine:$('method').value==='llm'?'llm':$('representation').value,auto_suggest:$('auto').checked,structured_gp:{ei_after_unique_measured_designs:Number($('threshold').value)},llm:{forward_model:$('forwardModel').value,inverse_model:$('inverseModel').value,n_samples:Number($('samples').value),manual_inverse_target:$('manualTarget').value===''?null:Number($('manualTarget').value),inverse_proposal_count:Number($('inverseCount').value),uncertainty_scalar:Number($('scalar').value),selector_mode:$('examples').value,forward_system_message:$('forwardPrompt').value===(state.config.llm.forward_system_message??'')?state.config.llm.forward_system_message:$('forwardPrompt').value,inverse_system_message:$('inversePrompt').value===(state.config.llm.inverse_system_message??'')?state.config.llm.inverse_system_message:$('inversePrompt').value}};}
for(const id of settingsFields){$(id).oninput=methodChanged;$(id).onchange=methodChanged;}
$('discardSettings').onclick=()=>{invalidateSettingsPreview();if(state){syncSettingsForm(state.config,current,true);toggle();}notice('Settings draft discarded. Saved campaign settings restored.');};
$('previewSettings').onclick=()=>guarded(async()=>{
 const requested=current,epoch=campaignEpoch,snapshot=settingsFingerprint(),changes=settingsChanges();invalidateSettingsPreview();const review= settingsReviewSequence;
 const r=await api('config-preview',{id:requested,changes});
 if(requested!==current||epoch!==campaignEpoch||review!==settingsReviewSequence||snapshot!==settingsFingerprint())return;
 settings=changes;settingsCampaign=requested;settingsSnapshot=snapshot;settingsAction='config';$('configPreview').textContent=JSON.stringify(r.after,null,2);$('applySettings').classList.remove('hidden');
});
$('applySettings').onclick=()=>guarded(async()=>{
 if(settingsSavePending)throw Error('A settings save is already in progress.');
 if(!settings||settingsCampaign!==current||settingsSnapshot!==settingsFingerprint())throw Error('Preview the current settings draft before applying it.');
 const requested=current,epoch=campaignEpoch,snapshot=settingsSnapshot,action=settingsAction,body=action==='preset-apply'?{id:requested,preset:settingsPreset}:{id:requested,changes:settings};
 settingsSavePending=true;updateDraftStatus();
 try{
 ++loadSequence;const saved=await api(action,body);
 if(requested!==current||epoch!==campaignEpoch)return;
 ++loadSequence;invalidateSettingsPreview();
 if(saved?.config)state=saved;
 if(snapshot===settingsFingerprint()){
  if(saved?.config)syncSettingsForm(saved.config,current,true);else{settingsBase=null;settingsBaseCampaign=null;}
 }else if(saved?.config){settingsBase=JSON.stringify(settingsConfigValues(saved.config));settingsBaseCampaign=current;}
 await load();notice('Settings saved. Measurement history is preserved.');
 }finally{settingsSavePending=false;updateDraftStatus();}
});
async function loadPresets(){
 const result=await api('presets',{},'GET');const presets=result.presets.filter(p=>p.data_schema==='moc');
 const selected=$('preset').value||'moc_five_gp',markup=presets.map(p=>`<option value="${esc(p.preset)}">${esc(p.name)} · ${esc(p.version)}</option>`).join('');
 $('preset').innerHTML=markup;$('preset').value=presets.some(p=>p.preset===selected)?selected:'moc_five_gp';$('resetPreset').innerHTML=markup;$('resetPreset').value=$('preset').value;
 await previewCreation(true);
}
async function previewCreation(resetBudget=false){
 const sequence=++presetSequence,preset=$('preset').value;if(resetBudget)budgetOverride=false;
 const overrides=budgetOverride?{new_measurement_budget:$('budget').value===''?null:Number($('budget').value)}:{};
 creationPreview=null;pairPreview=null;$('create').disabled=true;$('pair').disabled=true;
 const [chosen,gp,llm]=await Promise.all([api('preset-preview',{preset,overrides}),api('preset-preview',{preset:'moc_five_gp',overrides}),api('preset-preview',{preset:'moc_five_llm',overrides})]);
 if(sequence!==presetSequence||preset!==$('preset').value)return;
 creationPreview=chosen;pairPreview={gp,llm};if(!budgetOverride)$('budget').value=chosen.config.new_measurement_budget??'';
 $('creationPresetLabel').textContent=chosen.name+' · version '+chosen.version;$('creationConfig').textContent=JSON.stringify(chosen,null,2);$('pairConfig').textContent=JSON.stringify(pairPreview,null,2);updateCreationButtons();
}
$('preset').onchange=()=>guarded(()=>previewCreation(true));
$('budget').oninput=()=>{budgetOverride=true;creationPreview=null;pairPreview=null;++presetSequence;$('create').disabled=true;$('pair').disabled=true;};
$('budget').onchange=()=>guarded(()=>previewCreation(false));
$('resetPreset').onchange=invalidateSettingsPreview;
$('previewPresetReset').onclick=()=>guarded(async()=>{
 const requested=current,epoch=campaignEpoch,snapshot=settingsFingerprint(),preset=$('resetPreset').value;invalidateSettingsPreview();const review=settingsReviewSequence;
 const r=await api('preset-preview',{id:requested,preset});
 if(requested!==current||epoch!==campaignEpoch||review!==settingsReviewSequence||snapshot!==settingsFingerprint()||preset!==$('resetPreset').value)return;
 settings=r.config;settingsCampaign=requested;settingsSnapshot=snapshot;settingsAction='preset-apply';settingsPreset=preset;$('configPreview').textContent=JSON.stringify(r,null,2);$('applySettings').classList.remove('hidden');
});

$('bundleFile').onchange=()=>guarded(async()=>{const file=$('bundleFile').files[0];if(!file)return;const r=await api('import',{bundle_json:await file.text()});switchCampaign(r.campaign_id);await catalog();await load();notice('Portable campaign restored.');});$('replay').onclick=()=>guarded(async()=>{const standalone=(state.inverse_proposals||[]).some(r=>r.proposal_id===$('replayStep').value);const r=await api(standalone?'request-preview':'replay',{id:current,suggestion_id:$('replayStep').value,...(standalone?{role:'inverse'}:{})});$('replayResult').textContent=JSON.stringify(r,null,2);});$('cacheImport').onclick=()=>guarded(async()=>{requireSavedSettings();const r=await api('cache-import',{id:current,path:$('cachePath').value});$('cacheReport').textContent=JSON.stringify(r,null,2);});$('cachePrepare').onclick=()=>guarded(async()=>{requireSavedSettings();await api('cache-prepare',{id:current});await load();notice('Preparing missing embeddings. Progress is shown above.');});
const quantificationFields=['quantification_method','normalization','source_file','source_identifier','refinement_id','uncertainty_method','definition_note'];
function qualityValues(){return Object.fromEntries(quantificationFields.map(key=>[key,$('quality_'+key).value]));}
function fillQualityValues(record={}){const quality=record.measurement_quality||{};for(const key of quantificationFields)$('quality_'+key).value=quality[key]??(key==='quantification_method'?'historical_unspecified':'');}
function syncDefinitionForm(definition, campaignId) {
  const method = $("definitionMethod");
  const saved = {
    definitionMethod:
      definition.quantification_method === "historical_unspecified"
        ? ""
        : definition.quantification_method || "",
    definitionNormalization: definition.normalization || "",
    definitionNote: definition.definition_note || "",
    definitionHistoricalPolicy: ["exclude", "retain_with_justification"].includes(
      definition.historical_policy,
    )
      ? definition.historical_policy
      : "unresolved",
    definitionReason: "",
  };
  const values = JSON.stringify(saved);
  const current = JSON.stringify(
    Object.fromEntries(Object.keys(saved).map((id) => [id, $(id).value])),
  );
  // Polling may reveal a saved revision in another view, but keep local drafts.
  if (
    method.dataset.campaign === campaignId &&
    method.dataset.definitionValues &&
    current !== method.dataset.definitionValues
  )
    return;
  for (const [id, value] of Object.entries(saved)) $(id).value = value;
  method.dataset.campaign = campaignId;
  method.dataset.definitionValues = values;
}
$('applyDefinition').onclick=()=>guarded(async()=>{const requested=current;await api('measurement-definition',{id:requested,definition:{quantification_method:$('definitionMethod').value,normalization:$('definitionNormalization').value,definition_note:$('definitionNote').value},historical_policy:$('definitionHistoricalPolicy').value,reason:$('definitionReason').value});if(requested!==current)return;$('definitionMethod').dataset.campaign='';await load();notice('Measurement definition decision saved. Original values and provenance remain in history.');});
$('loadRefinement').onclick=()=>{const row=state.observations.find(r=>r.observation_id===$('reviseObservation').value);if(!row)return;for(const [id,key] of [['value','moc_wt_pct'],['sigma','moc_wt_pct_sigma'],['gof','gof'],['gap','closure_gap_wt_pct'],['gapOrigin','closure_gap_origin'],['note','source_note'],['moPhase','mo_wt_pct'],['mo2cPhase','mo2c_wt_pct'],['moo2Phase','moo2_wt_pct']])$(id).value=row[key]??'';$('phasesComplete').checked=Boolean(row.phase_accounting_complete);fillQualityValues(row);$('measurementQuality').open=true;notice('Loaded this observation for a documented refinement. Enter a revision reason and use Save refinement revision.');};
$('generateInverse').onclick=()=>guarded(async()=>{requireSavedSettings();const requested=current,epoch=campaignEpoch;await api('config',{id:requested,changes:{llm:{manual_inverse_target:$('manualTarget').value===''?null:Number($('manualTarget').value),inverse_proposal_count:Number($('inverseCount').value)}}});if(requested!==current||epoch!==campaignEpoch)return;await api('inverse-proposal',{id:requested});if(requested===current&&epoch===campaignEpoch)await load();});
$('requestPreviewSearch').onclick=()=>guarded(async()=>{const requested=current;const response=await fetch('/api/toolkit/candidate-search?campaign='+encodeURIComponent(requested)+'&q='+encodeURIComponent($('requestPreviewQuery').value));const result=await response.json();if(!response.ok)throw Error(result.error||'Candidate search failed');if(requested===current)$('requestPreviewCandidate').innerHTML=result.candidates.map(r=>`<option value="${esc(r.candidate_id)}">${esc(r.candidate_id)} · ${esc(r.procedure.slice(0,90))}</option>`).join('');});
$('requestPreview').onclick=()=>guarded(async()=>{const requested=current;const result=await api('request-preview',{id:requested,role:$('requestPreviewRole').value,candidate_id:$('requestPreviewSource').value==='recorded'?null:$('requestPreviewCandidate').value||null,suggestion_id:$('requestPreviewSource').value==='recorded'?$('replayStep').value:null});if(requested===current)$('requestPreviewResult').textContent=JSON.stringify(result,null,2);});
guarded(async()=>{await loadPresets();await catalog();if(current)await load();});
</script></html>"""
