# Defaults audit: corrected crystal continuation and reusable toolkit

This audit compares the effective configuration with the local handoff's `01_IMPLEMENTATION_PROMPT.md`, `03_EMBEDDINGS_AND_RUN_READINESS.md`, and `profiles/matched_campaign_requirements.json`, including the documented corrections to crystal snapshot `810c3f7d7ecc632e4cfd422977b10b471e526f82`. The handoff is reference input, not installed application content. The managed preset resolver is [campaign_config.py](../boicl/campaign_config.py); the main and focused views, CLI, exported configuration, and shared engine dispatcher use it.

The values below apply to new campaigns. Explicit settings and custom prompts in existing saved campaigns remain authoritative. An engine change does not turn a generic objective into MoC chemistry. No live provider call or laboratory result was used to validate these defaults.

## Campaign and LLM defaults

| Parameter                            | Effective new MoC default                                                                                                 | Source / implementation                                                                           |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| Objective                            | Maximize cubic MoC, wt%, bounds 0–100                                                                                     | Handoff continuation; `resolve_config`                                                            |
| Matched initialization               | M7 72.1, confirmed M12 83.8, M13 23.4; 7,776 candidates, 7,773 initially eligible                                         | Handoff confirmed observations; bundled source fixtures and `moc_import.py`                       |
| Older five outcomes                  | Archived, excluded from matched training; recipes eligible as planned quality repeats under the named source-reset policy | Corrected handoff; `campaign.py`                                                                  |
| Batch / replication                  | One sequential recommendation, one measurement per reservation, one independent campaign replicate                        | Handoff; `campaign_config.py`                                                                     |
| New-measurement budget               | Unlimited (`None`); zero is valid; initial observations do not consume it                                                 | Handoff continuation budget semantics; `campaign.py`                                              |
| Campaign seed                        | 616; suggestion sequence derives the step seed                                                                            | Handoff; `CampaignService.start`                                                                  |
| Automatic suggestion                 | Enabled after deliberate start and a saved measurement; opening/importing/settings alone do not start calls               | Handoff workflow; `campaign.py`                                                                   |
| Forward / inverse models             | GPT-4o / GPT-4o, independently configurable                                                                               | Crystal snapshot; `llm_engine.py`                                                                 |
| Forward / inverse temperature        | 0.7 / 0.7                                                                                                                 | Crystal snapshot; shared resolver and legacy role wrappers                                        |
| Forward samples / minimum accepted   | Five requested / two accepted; two through five may score, partial counts reported                                        | Snapshot plus corrected failure contract; `llm_engine.py`                                         |
| Inverse completions                  | One per BO step                                                                                                           | Crystal snapshot; `llm_engine.py`                                                                 |
| Forward / inverse token caps         | 256 / 576                                                                                                                 | Crystal snapshot; `llm_engine.py`, `AskTellFewShotTopk`                                           |
| Prediction weights                   | Equal; uncertainty scalar 1 preserves the empirical distribution exactly                                                  | Corrected handoff; `llm_engine.py`, `aqfxns.py`                                                   |
| Log probabilities / nucleus sampling | Log probabilities off; `top_p` omitted, so provider default applies                                                       | Audit of actual snapshot behavior; role wrappers do not forward the historical unused `top_p=0.5` |
| Observed example selector            | Nearest five; explicit all-observations option remains available                                                          | Crystal snapshot; shared resolver, `AskTellFewShot`, selector helper                              |
| Retrieval embedding                  | `text-embedding-3-large`, 3,072 dimensions, exact `experimental procedure: {original procedure}`                          | Handoff identity contract; `campaign.embedding_spec`, safe cache                                  |
| Retrieval                            | Full eligible pool; nearest 100 then MMR 16, lambda 0.5; zero random additions                                            | Crystal snapshot; `llm_engine.py`, `Pool` MMR defaults                                            |
| Acquisition                          | Empirical EI; xi 0 in objective units                                                                                     | Corrected handoff; `aqfxns.py`                                                                    |
| Optional UCB coefficient             | 0.5, inactive while EI is selected                                                                                        | Crystal snapshot; shared and fresh legacy settings                                                |
| Inverse target                       | Multiplier 1.2, jitter 0.05; nonnegative improvement applied in the optimization direction; bounded clamp                 | Corrected handoff; `resolve_inverse_target`                                                       |
| Target floor / ceiling               | None / None; zero is a valid explicit floor                                                                               | Corrected handoff; configuration and adapters                                                     |
| Zero-incumbent reference scale       | 100 for MoC's physical range                                                                                              | Corrected bounded-objective behavior                                                              |
| Provider request policy              | Eight total attempts, 0.5 s spacing, 10 s base cooldown; honor retry-after; SDK retries disabled                          | Explicit operational defaults, not a claim about notebook timing; `request_policy.py`             |

The shared dispatcher uses campaign `seed + suggestion_sequence` for LLM and structured-GP steps. Their nested seed defaults are also 616 and serve standalone engines; a nested override is not a second independent campaign seed. The embedding-GP projection has its own fixed `embedding_gp.seed`, default 616, which now controls initialization and participates in the saved projection fingerprint.

## GP defaults

| Parameter          | Structured MoC GP                                                                                                | Source / implementation                                                                      |
| ------------------ | ---------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Inputs             | Temperature, ramp, flow, hold, AMT:sucrose ratio, gas; no outcome/quality features                               | Handoff six-feature model; `structured_gp.py`                                                |
| Fixed transforms   | `(T-550)/350`, `(r-5)/10`, `(f-30)/70`, `(ln(h)-ln(0.5))/ln(20)`, `(log2(q)+1)/2`, H2=1/N2=0; `q` is AMT:sucrose | Handoff; full design-space scaling                                                           |
| Covariance / mean  | Matérn 5/2 ARD, six length scales and signal variance; zero mean after standardization                           | Crystal structured-GP source                                                                 |
| Priors             | Log length scale Normal(log(sqrt(6)), 0.75); log signal variance Normal(0, 1)                                    | Crystal structured-GP source                                                                 |
| Observation noise  | `sqrt(max(esd * max(GOF,1)^1, 0.5)^2 + gap^2)`                                                                   | Source quality model; missing required metadata errors, without silent zero substitution     |
| Outcome transform  | Padded logit, delta 0.65 percentage points, then standardization                                                 | Source transform; corrected bounded-posterior clamp before acquisition and inverse transform |
| Sampler            | Random-walk Metropolis; burn-in 1,000, retained draws 4,000, prediction thinning 20, proposal step 0.3           | Source algorithm; no sampler-family substitution                                             |
| Adaptation         | Once after burn-in: multiply step by `clip(burn_acceptance/0.3, 0.3, 3)`                                         | Source sampler behavior                                                                      |
| Coverage before EI | Maximin until ten distinct measured designs; then full eligible-pool EI                                          | Handoff coverage rule                                                                        |
| EI exploration     | 0.01 in standardized padded-logit units                                                                          | Source structured-GP acquisition                                                             |
| Computation        | Candidate chunks 512, quadrature nodes 96                                                                        | Implementation choices exposed in resolved configuration                                     |

| Parameter                | Optional embedding GP                                                                                                                | Source / implementation                                            |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------ |
| Inputs / embedding       | Exact bare original procedures; ada-002, 1,536 dimensions                                                                            | Separate handoff identity; no LLM prefix                           |
| Projection               | Isomap 32 components, five neighbors, seed 616; fixed full unique corpus including seeds                                             | Handoff full-corpus contract; `embedding_gp.py`                    |
| Small/disconnected pools | Deterministic dimension cap and connectivity adjustment, recorded in diagnostics                                                     | Necessary numerical fallback, not observation-dependent projection |
| Projection persistence   | Numeric non-pickle arrays; embedding identity, dimensions, neighbors, seed and implementation version enter fingerprint              | Validated-cache correction                                         |
| GP / posterior           | SingleTaskGP; fitted target transform and learned homoskedastic likelihood applied once; latent posterior reported in original units | Corrected public posterior path                                    |
| Scoring                  | Entire eligible pool, EI in original objective units, xi 0                                                                           | Corrected handoff; no inverse/chat call                            |

## Generic and compatibility boundaries

| Surface                             | Intentional behavior                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Generic shared presets              | Reuse numerical LLM, sampler, retrieval and request defaults. Objective defaults to `value`, units blank, bounds absent; users map objective/direction/features. No MoC phase instructions or fixed chemistry bounds.                                                                                                                    |
| Generic structured GP               | Explicit mapped linear/log/log2/categorical features and fixed pool scaling; coverage threshold two; reported uncertainty or explicit fallback sigma 1 in objective units. It does not apply MoC quality-noise rules. Bounded objectives derive logit padding as 0.0065 of range when created; unbounded objectives use standardization. |
| Generic inverse target              | Bounded range supplies reference scale. Unbounded zero-incumbent scale defaults to 1 in objective units and is configurable; this is a target parameter, not measurement uncertainty.                                                                                                                                                    |
| Fresh main legacy runner            | LLM, GPT-4o, EI, five samples, both temperatures 0.7, selector five, 3-large embeddings, one inverse proposal, MMR 16/100, UCB 0.5, projection 32/5. Generic names/units/bounds remain generic.                                                                                                                                          |
| Saved legacy runner                 | Explicit older models, temperatures, selector-all value, zero settings, budgets and custom prompts survive loading. A saved `optimizer=gpr` missing its embedding-model field receives ada-002; explicit models are retained.                                                                                                            |
| Fresh UI engine change              | Embedding GP selects ada-002 and LLM selects 3-large until the user explicitly edits the embedding model. Shared campaigns retain separate persisted model groups.                                                                                                                                                                       |
| Low-level `AskTellFewShot` / `Pool` | New constructor numerical defaults are GPT-4o, five prediction samples, selector five and 3-large embeddings. A supplied formatter stays exact; the general helper's identity formatter remains bare text for compatibility. Managed LLM campaigns explicitly supply the required prefixed template.                                     |
| Low-level helper calls              | Direct `get_llm` has a generic 128-token cap; direct `LLM` subclasses default to one completion. Role-specific forward/inverse wrappers set 256/576 and five/one. These are helper defaults, not campaign overrides.                                                                                                                     |
| Low-level embedding GP              | Explicit ada-002 and bare procedures remain separate from retrieval defaults. Projection seed is configurable, preserved and fingerprinted.                                                                                                                                                                                              |
| CLI                                 | `init` selects structured MoC; `--preset moc_llm` selects LLM; `--pair` creates the matched pair. Numerical settings come from the same resolver.                                                                                                                                                                                        |
| Legacy offline benchmark controls   | Their 30 iterations, five benchmark replicates, one initial point, random initialization and seed 0 are separate benchmark controls, not the live matched campaign's initialization, budget or seed.                                                                                                                                     |

## Concurrency, resume and verification

Each shared campaign has its own settings, observations, reservations and job. There is no two-campaign limit. Automatic durable checkpoints record accepted saved-state boundaries; named checkpoints use the same complete format. Restoring a checkpoint creates an independent campaign ID and preserves the source history. It does not resume a provider call or sampler midway through an instruction, and cannot reconstruct unsaved historical states from before checkpoint support.

Use one local application process for a state/cache directory. Multiple campaign threads in that process share a normalized absolute-directory reentrant lock. Cache preparation refreshes disk coverage under that lock before generating misses; three simultaneous LLM cache users are tested to generate each missing vector once. Raw and declared normalized caches retain their representation identity. Projection initialization is likewise serialized around the legacy numerical RNG and restores its previous state. Cross-process/distributed locking is not implemented.

`tests/test_defaults_contract.py` checks preset, CLI and main-adapter defaults, valid zeros, explicit saved legacy values and low-level constructors without external requests. Embedding tests check projection seed/cache identity and concurrent missing-only preparation. HTTP/service/browser tests cover independent campaigns and checkpoint copies. Exact historical prompt assets retain their source hashes; managed corrected prompts are separately versioned. Final broad-suite, packaging and browser results are recorded in the delivery report; numerical tests do not establish live provider access or laboratory performance.
